[pytest]
# Configures warning filters to keep test output clean and focused on business logic.
filterwarnings =
    # Ignores httpx internal deprecation warning triggered by older Starlette versions
    ignore:The 'app' shortcut is now deprecated:DeprecationWarning